(set-option :produce-assignments true)
(set-option :produce-proofs true)
(set-logic QF_UFLIA)
(declare-fun x () Int)
(declare-fun y () Int)
(assert (> x 0))
(assert (> y 0))
(assert (not (> (+ x y) 0)))
(check-sat)
